dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        jcenter() // Warning: this repository is going to shut down soon
    }
}
rootProject.name = "Painting Photographer"
include ':app'
include ':opencv'
project(':opencv').projectDir = new File(rootDir, 'libraries/OpenCV-android-sdk/sdk/')
